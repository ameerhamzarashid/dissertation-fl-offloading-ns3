cmake_minimum_required(VERSION 3.16)
project(ns3_unit_sanity LANGUAGES CXX)

# This is a stub CMake for CI sanity (no build steps by default).
# You can extend it to compile a tiny scenario that includes mec-sim.h headers.
